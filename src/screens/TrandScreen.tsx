import { SafeAreaView, Text } from "react-native";

const TrandScreen = () => {
    return (
        <SafeAreaView>
            <Text>asdf</Text>
        </SafeAreaView>
    );
}

export default TrandScreen;