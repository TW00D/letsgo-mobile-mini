import styled from "styled-components/native"

export const ProfileScreen = () => {
    
    const WelcomeText = styled.Text`
        font-size: 10px;
    `
    return (
        <WelcomeText>This is ProfileScreen Screen</WelcomeText>
    )    
}