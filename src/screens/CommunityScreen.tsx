import styled from "styled-components/native"

export const CommunityScreen = () => {
    
    const WelcomeText = styled.Text`
        font-size: 10px;
    `
    return (
        <WelcomeText>This is Community Screen</WelcomeText>
    )    
}