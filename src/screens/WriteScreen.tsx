import styled from "styled-components/native"

export const WriteScreen = () => {
    
    const WelcomeText = styled.Text`
        font-size: 10px;
    `
    return (
        <WelcomeText>This is Write Screen</WelcomeText>
    )    
}